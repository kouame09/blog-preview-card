//Ecris une fonction qui prend en entrée deux entier A et B et qui retourne la somme de leur carré

function sommeCarre(A, B) {
    return Math.pow(A, 2) + Math.pow(B, 2);
}

// Exemple d'utilisation
console.log(sommeCarre(2, 3));